apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slzj-game.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "slzj-game.name" . }}
    helm.sh/chart: {{ include "slzj-game.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "slzj-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
